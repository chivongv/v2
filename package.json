{
  "name": "v2",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "lerna run build --parallel",
    "dev": "lerna run dev --parallel",
    "dev-sanity": "lerna bootstrap && (cd cms/sanity && yarn run dev)",
    "dev-web": "lerna bootstrap && (cd web && yarn run dev)",
    "format": "lerna run format",
    "build-sanity": "lerna bootstrap && cd cms/sanity && npm run build",
    "build-web": "lerna bootstrap && (cd cms/sanity && SANITY_AUTH_TOKEN=$SANITY_DEPLOY_STUDIO_TOKEN npm run graphql-deploy) && (cd web && npm run build)",
    "graphql-deploy": "lerna run graphql-deploy",
    "lint": "lerna run lint",
    "postinstall": "lerna bootstrap",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@sanity/cli": "^1.149.9",
    "lerna": "^3.20.2"
  }
}

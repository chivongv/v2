import ToggleMode from '../components/ToggleMode';
import Layout from '../components/Layout';

const Home = () => {
  return (
    <div>
      <Layout title="Home">
        <ToggleMode />
      </Layout>
    </div>
  );
};

export default Home;
